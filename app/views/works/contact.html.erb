
		<div class="booyah-box col-10 offset-1">
			<h1>Enter Your Details Sir</h1>
			<%= simple_form_for @work do |f| %>
				<%= f.input :name %> 
				<%= f.input :email_address %> 
				<%= f.input :new_message %> 
				<br />
				<%= f.submit 'Contact Me', class: 'btn btn-primary' %>
			<% end %>
		</div><!--top-banner--> 
	